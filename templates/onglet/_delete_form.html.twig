<form method="post" action="{{ path('onglet_delete', {'id': onglet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ onglet.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
