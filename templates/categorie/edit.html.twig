{% extends 'base.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    {% block menu %}
            {% include "components/navbar-admin.html.twig" %}
    {% endblock %}
    <h1 class="text-center mt-5">Éditer une Categorie</h1>

    {{ include('categorie/_form.html.twig', {'button_label': 'Sauvegarder'}) }}


    <div class="d-flex justify-content-center mt-3">
    {{ include('categorie/_delete_form.html.twig') }}
    </div>

    <div class="d-flex justify-content-around" style="width: 100vw;">
        <a class="btn btn-outline-primary mt-5" href="{{ path('categorie_index') }}">Revenir à la liste des catégories</a>
        <a class="btn btn-outline-primary mt-5" href="{{ path('administration') }}">Revenir à l'acceuil Administration</a>
    </div>


{% endblock %}
