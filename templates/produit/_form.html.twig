<div class="container mt-5">
    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.prix) }}</div>
    </div>
    <div class="row mb-3">
        <div class="col">{{ form_row(form.description) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.categories) }}</div>
        <div class="col">{{ form_row(form.imageFile) }}</div>
    </div>
        <div class="d-flex justify-content-center mt-3">
            <button class="btn btn-outline-success">{{ button_label|default('Sauvegarder') }}</button>
        </div>
        {{ form_end(form) }}
</div>
