<div class="container mt-5">
    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.prix) }}</div>
    </div>
    <div class="row mb-3">
        <div class="col">{{ form_row(form.description) }}</div>
    </div>
    <h5>Disponible en livraison : </h5>
    <div class="row mb-3">
        <div class="col">{{ form_row(form.ubereats) }}</div>
        <div class="col">{{ form_row(form.deliveroo) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.categories) }}</div>
        <div class="col">{{ form_row(form.imageFile) }}</div>
    </div>
        <div class="d-flex justify-content-center mt-3">
            <button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
        </div>
        {{ form_end(form) }}
</div>
