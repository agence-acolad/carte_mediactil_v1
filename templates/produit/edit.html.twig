{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    {% block menu %}
            {% include "components/navbar-admin.html.twig" %}
    {% endblock %}
    <h1 class="text-center mt-5">Éditer</h1>



        {{ include('produit/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="d-flex justify-content-center mt-3">
        {{ include('produit/_delete_form.html.twig') }}
    </div>
        <a class="btn btn-outline-primary" href="{{ path('produit_index') }}">Revenir à la liste des produits</a>

{% endblock %}
